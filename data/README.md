# Data Directory

For this Monte Carlo simulation, no external dataset is required as the data (random points) is generated on-the-fly during execution.

This directory is kept to maintain the standard project structure.
